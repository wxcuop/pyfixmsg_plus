from setuptools import setup, find_packages

setup(
    name="pyfixmsg",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "six>=1.12.0",
        # Add other dependencies here
    ],
)
