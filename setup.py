from setuptools import setup, find_packages

setup(
    name="pyfixmsg_plus",
    version="0.0.1",
    packages=find_packages(),
    install_requires=[
        "six>=1.12.0",
        # Add other dependencies here
    ],
)
